CREATE TABLE [dbo].[RigesterCustomer]
(
	[Id] INT NOT NULL PRIMARY KEY, 
    [first name ] VARCHAR(MAX) NOT NULL, 
    [Last name ] VARCHAR(MAX) NOT NULL, 
    [Phone number ] INT NOT NULL, 
    [address] VARCHAR(MAX) NOT NULL
)
